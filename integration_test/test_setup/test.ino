void setup() {
    // Set pin mode.
    pinMode(0, OUTPUT);
    pinMode(3, INPUT);
    pinMode(13, OUTPUT);

    // Set pin value.
    digitalWrite(0, HIGH);
    digitalWrite(13, LOW);
}

void loop() {

}
